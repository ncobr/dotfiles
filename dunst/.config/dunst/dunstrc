[global]
    monitor = 0
    follow = mouse

    # POSICIÓN: abajo centrado
    origin = bottom-center
    offset = (0, 80)
    width = (0, 450)
    height = (0, 300)

    # Transparencia y estilo
    transparency = 15
    padding = 12
    horizontal_padding = 12
    text_icon_padding = 8
    separator_height = 2

    # Estilo visual (macOS style)
    frame_width = 1
    frame_color = "#67623C30"  # color1 con transparencia
    separator_color = frame
    corner_radius = 12
    corners = all

    # Fuente
    font = SF Pro Display 11

    # Texto
    line_height = 0
    markup = full
    format = "<b>%s</b>\n%b"
    alignment = left
    vertical_alignment = center
    show_age_threshold = 60
    word_wrap = yes
    ellipsize = middle
    ignore_newline = no
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = yes

    # Iconos
    enable_recursive_icon_lookup = true
    icon_theme = "Adwaita, hicolor, MacTahoe-dark, Kora"
    icon_position = left
    min_icon_size = 24
    max_icon_size = 48
    icon_corners = all
    icon_corner_radius = 8

    # Barra de progreso
    progress_bar = true
    progress_bar_height = 8
    progress_bar_frame_width = 1
    progress_bar_min_width = 150
    progress_bar_max_width = 300
    progress_bar_corner_radius = 4
    progress_bar_corners = all

    # Historial y navegación
    sticky_history = yes
    history_length = 20
    indicate_hidden = yes
    dmenu = /usr/bin/rofi -dmenu -p dunst:
    browser = /usr/bin/xdg-open

    # Comportamiento
    always_run_script = true
    title = Dunst
    class = Dunst
    timeout = 5
    background = "#14100c90"      # color0 con opacidad
    foreground = "#bbbca7"        # foreground de pywal
    force_xinerama = false

[urgency_low]
    background = "#14100c95"      # color0 con opacidad
    foreground = "#bbbca7"
    frame_color = "#67623C40"     # color1 translúcido
    timeout = 4
    icon = ""

[urgency_normal]
    background = "#14100c95"
    foreground = "#bbbca7"
    frame_color = "#716F4A40"     # color3 translúcido
    timeout = 6
    icon = ""

[urgency_critical]
    background = "#67623C95"      # color1 con opacidad
    foreground = "#14100c"        # texto negro fuerte
    frame_color = "#A9863640"     # color6 translúcido
    timeout = 0
    icon = ""

