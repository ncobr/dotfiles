# Set the layout for the first tab
layout splits

# Create the first tab, running a default shell
new_tab MyProject
cd ~/my-project
launch

# Create a second tab for notes and monitoring
new_tab Monitoring
cd ~/my-project

# Split the "Monitoring" tab vertically
launch --location=vsplit

# Run 'htop' in the right split
launch htop
